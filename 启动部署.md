# 启动部署文档

## 目录

- [项目介绍](#项目介绍)
- [环境要求](#环境要求)
- [项目初始化](#项目初始化)
- [本地开发](#本地开发)
- [生产部署](#生产部署)
- [常见问题](#常见问题)

---

## 项目介绍

### 项目概述

这是一个基于 **Nuxt 3 + MySQL** 构建的全栈管理系统，具有完整的用户管理、权限控制、文章管理和登录日志功能。

### 核心特性

- ✨ **前后端一体化架构** - 使用 Nuxt 3 的服务端能力
- 🔐 **完善的权限管理** - 基于 RBAC 的角色权限系统
- 📝 **文章管理系统** - 支持创建、编辑、发布、归档
- 📊 **登录日志审计** - 记录所有登录行为
- 🎨 **现代化 UI** - 响应式设计，支持移动端
- 🔒 **安全认证** - JWT Token + bcrypt 密码加密

### 技术架构

```
┌─────────────────────────────────────┐
│         前端 (Vue 3 + Nuxt 3)        │
│  - 页面路由                          │
│  - 组件系统                          │
│  - 状态管理                          │
└─────────────────────────────────────┘
                 ↕
┌─────────────────────────────────────┐
│      Nuxt Server API (Nitro)        │
│  - 路由处理                          │
│  - 中间件                            │
│  - 服务端渲染                         │
└─────────────────────────────────────┘
                 ↕
┌─────────────────────────────────────┐
│         MySQL 数据库                  │
│  - 用户管理                          │
│  - 文章管理                          │
│  - 登录日志                          │
│  - 系统设置                          │
└─────────────────────────────────────┘
```

### 业务模块

#### 1. 用户管理模块

- **用户注册/登录**：支持用户名密码登录
- **用户信息管理**：查看、编辑用户信息
- **角色管理**：管理员、编辑、普通用户三种角色
- **状态管理**：启用/停用用户账号

#### 2. 文章管理模块

- **文章创建**：支持富文本内容和图片插入
- **文章编辑**：修改文章内容和属性
- **文章发布**：草稿、发布、归档状态管理
- **文章统计**：记录文章浏览次数

#### 3. 文件上传模块

- **图片上传**：支持 JPG、PNG、GIF、WebP 格式
- **文件管理**：自动生成 UUID 文件名，避免冲突
- **存储管理**：开发环境和生产环境分别配置存储路径

#### 4. 权限管理模块

- **角色权限**：基于角色的访问控制
- **权限验证**：前端路由 + 后端 API 双重验证
- **操作审计**：登录日志记录所有登录行为

#### 5. 系统设置模块

- **站点配置**：站点名称、描述等
- **系统参数**：最大用户数、会话超时时间
- **灵活扩展**：可自定义配置项

---

## 环境要求

### 开发环境

| 软件/工具 | 版本要求 | 说明 |
|-----------|---------|------|
| **Node.js** | >= 18.0.0 | JavaScript 运行环境 |
| **npm** | >= 9.0.0 | 包管理器 |
| **MySQL** | >= 8.0 | 数据库 |
| **Git** | >= 2.0 | 版本控制（可选） |

### 推荐工具

- **VS Code**：代码编辑器
- **TablePlus** 或 **Navicat**：数据库管理工具
- **Postman** 或 **Insomnia**：API 测试工具

---

## 项目初始化

### 1. 克隆项目（如需要）

```bash
git clone <repository-url>
cd nuxt_database
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置环境变量

创建 `.env` 文件（参考 `.env.example`）：

```env
# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=nuxt_app

# 文件上传配置
UPLOAD_ADRES=http://localhost:3000

# 服务端口（可选，默认3000）
PORT=3000
```

**重要**：请将 `your_password_here` 替换为你的 MySQL 密码。

### 4. 创建数据库

#### 方式一：使用 MySQL 命令行

```bash
# 登录 MySQL
mysql -u root -p

# 执行初始化脚本
source /path/to/database/init.sql
```

#### 方式二：使用 GUI 工具

1. 打开 TablePlus 或 Navicat
2. 创建新连接，连接到 MySQL
3. 执行 `database/init.sql` 文件中的 SQL 语句

#### 方式三：使用命令行直接导入

```bash
mysql -u root -p < database/init.sql
```

### 5. 验证数据库连接

```bash
# 启动开发服务器
npm run dev
```

如果看到以下输出，说明数据库连接成功：

```
数据库连接池已创建
数据库连接测试成功
```

### 6. 生成管理员密码（可选）

如果想修改默认管理员密码，运行：

```bash
node database/generate-password.js
```

将输出的加密密码更新到数据库中。

---

## 本地开发

### 启动开发服务器

```bash
npm run dev
```

服务启动后，访问：

- **前端页面**：http://localhost:3000
- **API 接口**：http://localhost:3000/api/*

### 默认账号

```
用户名：admin
密码：123456
角色：管理员
```

**⚠️ 重要**：首次登录后请立即修改密码！

### 开发目录结构

```
nuxt_database/
├── assets/              # 静态资源
├── composables/         # 组合式函数
├── layouts/             # 布局组件
├── middleware/          # 中间件
├── pages/               # 页面路由
│   ├── index.vue       # 首页
│   ├── login.vue       # 登录页
│   ├── users/          # 用户管理
│   ├── posts/          # 文章管理
│   └── logs/           # 登录日志
├── plugins/             # 插件
├── public/              # 公共资源
├── server/              # 服务端代码
│   ├── api/            # API 接口
│   ├── config/         # 配置文件
│   ├── db/             # 数据库操作
│   ├── middleware/     # 中间件
│   └── utils/          # 工具函数
├── types/               # 类型定义
├── .env                 # 环境变量
├── nuxt.config.ts      # Nuxt 配置
└── package.json        # 项目配置
```

### 开发规范

#### 1. 代码风格

- 使用 TypeScript 进行类型定义
- 遵循 ESLint 和 Prettier 配置
- 使用组合式 API（Composition API）

#### 2. 命名规范

- **文件名**：kebab-case（如：`user-list.vue`）
- **组件名**：PascalCase（如：`UserList.vue`）
- **变量名**：camelCase（如：`userName`）
- **常量名**：UPPER_SNAKE_CASE（如：`MAX_USERS`）

#### 3. Git 提交规范

```bash
# 功能开发
git commit -m "feat: 添加文章搜索功能"

# Bug 修复
git commit -m "fix: 修复用户登录时的权限验证问题"

# 文档更新
git commit -m "docs: 更新API文档"

# 样式调整
git commit -m "style: 优化首页布局"
```

### 常用开发命令

```bash
# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview

# 类型检查
npm run typecheck

# 代码格式化
npm run format

# 代码检查
npm run lint
```

---

## 生产部署

### 准备工作

#### 1. 修改环境变量

创建生产环境的 `.env` 文件：

```env
# 生产数据库配置
DB_HOST=your-production-host
DB_PORT=3306
DB_USER=your-production-user
DB_PASSWORD=your-production-password
DB_NAME=nuxt_app

# 文件上传配置（重要！）
UPLOAD_ADRES=http://your-domain-or-ip

# 生产环境配置
NODE_ENV=production
PORT=3000
```

#### 2. 修改默认密码

**⚠️ 必须修改默认管理员密码！**

```bash
# 生成新密码
node database/generate-password.js

# 更新数据库
mysql -u root -p nuxt_app
UPDATE users SET password = '新生成的加密密码' WHERE username = 'admin';
```

### 部署方式

#### 方式一：Vercel 部署（推荐）

**优点**：零配置、自动 HTTPS、全球 CDN

1. **准备工作**
   - 注册 [Vercel 账号](https://vercel.com)
   - 准备一个 MySQL 数据库（推荐使用 PlanetScale、AWS RDS 等）

2. **导入项目**
   ```bash
   # 安装 Vercel CLI
   npm i -g vercel

   # 登录
   vercel login

   # 部署
   vercel
   ```

3. **配置环境变量**
   在 Vercel 控制台中添加环境变量：
   - `DB_HOST`
   - `DB_PORT`
   - `DB_USER`
   - `DB_PASSWORD`
   - `DB_NAME`

4. **重新部署**
   ```bash
   vercel --prod
   ```

#### 方式二：Docker 部署

1. **创建 Dockerfile**

   在项目根目录创建 `Dockerfile`：

   ```dockerfile
   FROM node:18-alpine

   # 安装依赖
   WORKDIR /app
   COPY package*.json ./
   RUN npm ci

   # 复制源代码
   COPY . .

   # 构建项目
   RUN npm run build

   # 暴露端口
   EXPOSE 3000

   # 启动服务
   CMD ["npm", "run", "start"]
   ```

2. **创建 .dockerignore**

   ```
   node_modules
   .nuxt
   .env
   .env.*
   ```

3. **构建镜像**

   ```bash
   docker build -t nuxt-database .
   ```

4. **运行容器**

   ```bash
   docker run -d \
     --name nuxt-app \
     -p 3000:3000 \
     --env-file .env \
     nuxt-database
   ```

#### 方式三：传统服务器部署

1. **服务器环境要求**
   - Ubuntu 20.04+ 或 CentOS 7+
   - Node.js 18+
   - MySQL 8.0+
   - Nginx（可选，用于反向代理）

2. **配置文件上传目录**

生产环境需要创建文件上传目录：

```bash
# 创建上传文件存储目录
mkdir -p /var/www/nuxt_database/files/uploads

# 设置目录权限（确保应用有写入权限）
chmod 755 /var/www/nuxt_database/files/uploads
```

在 `.env` 文件中配置文件服务器地址：

```env
# 文件上传配置
UPLOAD_ADRES=http://your-domain-or-ip:8888
```

3. **配置 Nginx（可选）**

如果使用 Nginx 作为反向代理，需要配置上传文件访问路径：

```nginx
server {
    listen 80;
    server_name your-domain.com;

    # API 代理
    location /api/ {
        proxy_pass http://localhost:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    # 上传文件访问路径
    location /uploads/ {
        alias /var/www/nuxt_database/files/uploads/;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # 其他路由
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

4. **上传代码**

   ```bash
   # 方式一：使用 Git
   git clone <repository-url>
   cd nuxt_database

   # 方式二：使用 SCP
   scp -r ./nuxt_database user@server:/var/www/

   # 方式三：使用 FTP 工具
   ```

5. **安装依赖**

   ```bash
   cd /var/www/nuxt_database
   npm install --production
   ```

6. **构建项目**

   ```bash
   npm run build
   ```

7. **使用 PM2 运行**

   ```bash
   # 安装 PM2
   npm install -g pm2

   # 启动应用
   pm2 start npm --name "nuxt-app" -- start

   # 保存 PM2 配置
   pm2 save

   # 设置开机自启
   pm2 startup
   ```

#### 方式四：使用 PM2 配置文件

在项目根目录创建 `ecosystem.config.js`：

```javascript
module.exports = {
  apps: [{
    name: 'nuxt-database',
    script: 'node',
    args: './.output/server/index.mjs',
    instances: 'max',
    exec_mode: 'cluster',
    env: {
      NODE_ENV: 'production',
      PORT: 3000
    }
  }]
}
```

启动：

```bash
pm2 start ecosystem.config.js
```

---

## 常见问题

### 1. 数据库连接失败

**错误信息**：`Access denied for user`

**解决方案**：
- 检查 `.env` 文件中的数据库配置
- 确认 MySQL 用户名和密码是否正确
- 检查 MySQL 服务是否启动

```bash
# 检查 MySQL 状态
sudo systemctl status mysql

# 启动 MySQL
sudo systemctl start mysql
```

### 2. 端口被占用

**错误信息**：`Error: listen EADDRINUSE: address already in use :::3000`

**解决方案**：

```bash
# 方式一：修改端口
PORT=3001 npm run dev

# 方式二：杀死占用端口的进程
lsof -ti:3000 | xargs kill -9
```

### 3. Token 过期问题

**现象**：用户频繁被踢出登录

**解决方案**：
- 增加会话超时时间（系统设置中调整 `sessionTimeout`）
- 检查客户端系统时间是否正确
- 检查 Token 是否正确存储在 localStorage

### 4. 权限验证失败

**现象**：登录后仍然提示权限不足

**解决方案**：
- 清除浏览器缓存和 localStorage
- 检查 Token 是否正确发送到后端
- 查看浏览器控制台的网络请求

### 5. 生产环境构建失败

**错误信息**：`Cannot find module 'xxx'`

**解决方案**：
```bash
# 清除缓存重新构建
rm -rf .nuxt node_modules
npm install
npm run build
```

### 6. 数据库表不存在

**错误信息**：`Table 'nuxt_app.xxx' doesn't exist`

**解决方案**：
```bash
# 重新执行初始化脚本
mysql -u root -p nuxt_app < database/init.sql
```

### 7. CORS 跨域问题

**现象**：前端请求 API 时出现跨域错误

**解决方案**：在 `nuxt.config.ts` 中配置 CORS：

```typescript
export default defineNuxtConfig({
  nitro: {
    experimental: {
      openAPI: true
    }
  },
  routeRules: {
    '/api/**': { cors: true, headers: { 'Access-Control-Allow-Origin': '*' } }
  }
})
```

### 8. 文件上传失败

**现象**：上传图片时提示失败或文件无法访问

**解决方案**：

1. **检查上传目录权限**

```bash
# 检查目录是否存在
ls -la /var/www/nuxt_database/files/uploads

# 重新创建目录并设置权限
mkdir -p /var/www/nuxt_database/files/uploads
chmod 755 /var/www/nuxt_database/files/uploads
```

2. **检查环境变量配置**

确认 `.env` 文件中 `UPLOAD_ADRES` 配置正确：

```env
# 开发环境
UPLOAD_ADRES=http://localhost:3000

# 生产环境（替换为实际域名或IP）
UPLOAD_ADRES=http://your-domain-or-ip:8888
```

3. **检查 Nginx 配置**（如果使用 Nginx）

确保 Nginx 配置了 `/uploads/` 路径：

```nginx
location /uploads/ {
    alias /var/www/nuxt_database/files/uploads/;
    expires 30d;
    add_header Cache-Control "public, immutable";
}
```

4. **验证文件类型**

确保上传的是支持的图片格式：`.jpg`, `.jpeg`, `.png`, `.gif`, `.webp`

---

## 性能优化建议

### 1. 数据库优化

- 为常用查询字段添加索引
- 定期清理登录日志（建议保留最近3个月）
- 使用连接池避免频繁创建连接

### 2. 应用优化

- 启用 Gzip 压缩
- 使用 CDN 加载静态资源
- 开启 SSR 缓存
- 使用图片懒加载

### 3. 服务器优化

- 使用 PM2 集群模式充分利用多核 CPU
- 配置 Nginx 反向代理和负载均衡
- 启用 HTTP/2
- 配置适当的缓存策略

---

## 监控与日志

### 1. 应用监控

推荐使用以下工具监控应用状态：

- **PM2 Plus**：进程监控
- **Sentry**：错误追踪
- **Grafana + Prometheus**：性能监控

### 2. 日志管理

生产环境建议配置日志系统：

- 使用 Winston 或 Pino 记录日志
- 定期归档日志文件
- 设置日志级别（开发环境 debug，生产环境 warn）

---

## 备份策略

### 1. 数据库备份

```bash
# 备份整个数据库
mysqldump -u root -p nuxt_app > backup_$(date +%Y%m%d).sql

# 只备份数据（不包含结构）
mysqldump -u root -p nuxt_app --no-create-info > data_$(date +%Y%m%d).sql

# 恢复数据库
mysql -u root -p nuxt_app < backup_20250120.sql
```

### 2. 自动备份脚本

创建 `scripts/backup.sh`：

```bash
#!/bin/bash
BACKUP_DIR="/var/backups/nuxt_app"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
mysqldump -u root -p[your_password] nuxt_app | gzip > $BACKUP_DIR/backup_$DATE.sql.gz
find $BACKUP_DIR -type f -mtime +30 -delete
```

添加到 crontab：

```bash
# 每天凌晨 2 点自动备份
0 2 * * * /path/to/scripts/backup.sh
```

---

## 更新升级

### 1. 拉取最新代码

```bash
git pull origin main
```

### 2. 安装依赖

```bash
npm install
```

### 3. 重新构建

```bash
npm run build
```

### 4. 重启服务

```bash
# PM2 方式
pm2 restart nuxt-app

# Docker 方式
docker-compose down
docker-compose up -d --build

# 传统方式
sudo systemctl restart nginx
```

---

## 技术支持

如有问题，请通过以下方式联系：

- 📧 Email: support@example.com
- 📱 微信: your-wechat-id
- 💬 GitHub Issues: [项目地址]

---

## 许可证

本项目仅供学习和研究使用。

---

**最后更新时间**：2025-01-20
